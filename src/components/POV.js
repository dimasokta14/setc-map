import React, {useMemo} from 'react';
import {PerspectiveCamera} from 'drei'

const POV = ({position, children}) => {
  const NUM = 3
  const positions = useMemo(() => {
    const pos = []
    const half = (NUM - 1)/2
    
  },[])

  return (
    <div>
      
    </div>
  )
}

export default POV
